{
  "tasks": {
    "dev": "deno run -A npm:vite",
    "dev:electron": "npx electron electron/dev.js",
    "dev:window": "deno task dev & npx electron electron/dev.js",
    "build": "deno run -A npm:vite build",
    "build:electron": "python3 python/build_electron.py",
    "build:android": "python3 python/build_android.py",
    "install": "npm install @sveltejs/vite-plugin-svelte vite && npm install fdir sass canvas maxrects-packer minimatch types semver && npm install electron@31.3.0 electron-package @capacitor/android",
    "clear": "rm -rf deno.lock && rm -rf package-lock.json && rm -rf package.json && rm -rf node_modules && npm cache clean --force"
  },
  "nodeModulesDir": "manual",
  "importMap": "../common/deps.json",
  "compilerOptions": {
    "lib": ["dom", "deno.ns"]
  }
}
